apiVersion: apps/v1
kind: Deployment
metadata:
  name: users
spec:
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: "genorred/users-pepp:latest"
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: global-configmap
            - secretRef:
                name: server-secret
            - secretRef:
                name: users-secret
      #     readinessProbe:
      #       tcpSocket:
      #         port: 8080
      #       initialDelaySeconds: 5
      #       periodSeconds: 5
      #     livenessProbe:
      #       httpGet:
      #         path: /status
      #         port: 8080
      # imagePullSecrets:
      #   - name: dpr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: users
spec:
  selector:
    app: users
  ports:
    - port: 8080
      targetPort: 8080
