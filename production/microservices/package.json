{
  "private": true,
  "scripts": {
    "build": "pnpm --filter '!libs/shared' run build",
    "start:dev": "pnpm --filter !shared run start:dev",
    "test": "pnpm run -r test",
    "lint": "pnpm run -r lint"
  }
}